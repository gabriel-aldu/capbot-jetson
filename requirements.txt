# Dependencias del servicio Jetson — pinned para Python 3.6
#
# NOTA: Python 3.6 llegó a EOL el 2021-12-23. Estas versiones son
# las últimas que soportan 3.6. Instalar con:
#   pip install -r requirements.txt

# Backport oficial de dataclasses (stdlib a partir de 3.7).
dataclasses==0.8; python_version < "3.7"

# websockets 10.0 dejó de soportar 3.6. 9.1 es la última compatible.
# Cambio de API notable: handler recibe (ws, path), no (ws,).
websockets==9.1

# pyserial no tiene problemas con 3.6.
pyserial>=3.4,<4.0

# Percepción de obstáculos (perception/): numpy para los frames/postproceso
# de la DNN. OJO: tensorrt, pycuda, cv2 (OpenCV) y gi (GStreamer) NO van por
# pip — vienen con JetPack 4.6 / apt en la Jetson. En un PC sin ellos el
# servicio arranca igual con la percepción deshabilitada.
# (pip en 3.6 resuelve solo la última compatible: numpy 1.19.5)
numpy